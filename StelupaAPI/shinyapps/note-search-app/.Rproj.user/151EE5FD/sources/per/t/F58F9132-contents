harmony_filter_server <- function(input, output, session) {
  
  return(
    list(
      instrument_groupings = reactive({ input$instrument_groupings }),
      min_number_of_consecutive_bars = reactive({ input$min_number_of_consecutive_bars }),
      instrumentation_measure_range = reactive({ input$instrumentation_measure_range })
    ))
}
