score_detail_server <- function(input, output, session) {
  
  return(
    list(
      score_choice = reactive({ input$score_choice }),
      measure_location = reactive({ input$measure_location })
    ))
}
