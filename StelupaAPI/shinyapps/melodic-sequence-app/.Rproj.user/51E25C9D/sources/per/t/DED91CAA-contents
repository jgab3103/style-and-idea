app_header_ui <- function(id) {
  
  ns <- NS(id)
  
  tagList(
    
    fluidRow(
      br(),
      column(
        9,
        h5("Exploring melodic sequences")
      ),
      column(
        3,
        bookmarkButton(
          label = "Bookmark search settings")
      )
    ))
}